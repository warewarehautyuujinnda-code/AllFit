# FitLog の開発ルール

このアプリは作者本人が毎日使っている記録アプリで、入っているのは実データ（体重・食事・トレーニング）。
やり直しのきかない変更を入れないことを最優先にする。

## 絶対ルール: 利用者のデータを消さない

### 種目（exercise）の削除は「一覧から隠す」だけ

- 種目選択画面の削除は **論理削除**。`exercise.hiddenAt` に日時を入れて一覧に出さなくするだけで、
  種目の定義・説明・その種目の記録（`strength` / `strength_set`）・メモは DB にそのまま残す。
- そのため `ExerciseDao` には削除のクエリを置かない（`@Delete`、`DELETE FROM exercise`、
  行の削除を伴う `OnConflictStrategy.REPLACE` も使わない）。
  `ExerciseRetentionRuleTest` がソースを走査してこの決まりを検査している。
- 「全データ削除」(FR-13) でも種目の定義は消さない。消すのは記録だけ。
- 同じ名前で種目を追加し直すと、説明も記録もそのままに一覧へ戻る（`hiddenAt` を戻すだけ）。

### そのほか

- Room のマイグレーションで破壊的フォールバック（`fallbackToDestructiveMigration`）を使わない。
  列・テーブルの追加と、データを引き継ぐ作り直しだけで上げる。
- 読み込み（FR-12）は全置換ではなくマージ。既にある記録や、利用者が書いた説明を
  古いバックアップで上書きして失わせない（`mergeImportedExercise` 参照）。

## データ設計の方針: AI に渡して読めること

書き出した JSON（FR-11）はそのまま AI に渡して分析させる前提。構造の説明が要らない形を保つ。

- **種目（`exercise`）= オブジェクト**。「その種目がどういうものか」を持つ。
  説明（`description`）はプロパティのひとつで、今後プロパティを増やすときもこのテーブルに足す。
- **記録（`strength` + `strength_set`）= 出来事**。「いつ・何を・どれだけやったか」。
  種目とは種目名（`strength.ex` = `exercise.name`）で紐づく。
- **メモ（`strength.memo`）= その回の主観**。感覚・感想・フォームで意識したこと。
  種目の説明（種目とは何か）と対になる情報として積み上げる。
- 新しくテーブルを足したら、書き出し（`FitLogBackup`）と読み込みにも必ず通す。
  片方だけに存在するデータを作らない。

## 画面まわり

- 表示文言は日本語。文字列は各 Composable に直接書く（`strings.xml` は使っていない）。
- 種目の一覧は「記録していない種目は出さない」方針（Issue #47）。
  ただし説明を書いて追加した種目は、まだ記録が無くても部位タブに出す。
